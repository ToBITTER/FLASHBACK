import type{Metadata}from"next";import{CapsuleExperience}from"@/components/capsule-experience";import{db}from"@/lib/db";export async function generateMetadata({params}:{params:Promise<{code:string}>}):Promise<Metadata>{const code=(await params).code.toUpperCase(),capsule=await db.memoryCapsule.findUnique({where:{code},select:{title:true,description:true,owner:{select:{username:true}}}});if(!capsule)return{title:"Memory Capsule · FLASHBACK"};return{title:`${capsule.title} · Memory Capsule`,description:`${capsule.owner.username} preserved this memory: ${capsule.description}`,openGraph:{type:"website",title:`Can you remember ${capsule.title}?`,description:`Open ${capsule.owner.username}'s FLASHBACK Memory Capsule.`},twitter:{card:"summary_large_image",title:`Can you remember ${capsule.title}?`,description:`Open ${capsule.owner.username}'s FLASHBACK Memory Capsule.`}}}export default async function Page({params,searchParams}:{params:Promise<{code:string}>;searchParams:Promise<Record<string,string|undefined>>}){const[{code},query]=await Promise.all([params,searchParams]);return<CapsuleExperience code={code.toUpperCase()} created={query.created==="1"}/>}
