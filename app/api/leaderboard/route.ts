import{NextResponse}from"next/server";import{db}from"@/lib/db";export async function GET(){const stats=await db.userStats.findMany({take:50,orderBy:{totalScore:"desc"},include:{user:{select:{username:true}}}});return NextResponse.json({players:stats.map((row,index)=>({rank:index+1,username:row.user.username,totalScore:row.totalScore,weeklyScore:row.weeklyScore,games:row.gamesPlayed}))});}
