"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";import{Share2,Users}from"lucide-react";export function Rooms(){const[code,setCode]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const router=useRouter();async function act(action:"create"|"join"){setBusy(true);setError("");const r=await fetch("/api/rooms",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(action==="create"?{action}:{action,code})}),d=await r.json();setBusy(false);if(r.status===401)return router.push("/account");if(!r.ok)return setError(d.error);router.push(`/rooms/${d.code}`)}return<main className="rooms-page"><header><span>LIVE MULTIPLAYER</span><h1>CALL YOUR<br/>PEOPLE.</h1><p>Same ten memories. Live scores. No Googling. Settle who actually remembers Naija.</p></header><section><article><Users/><h2>HOST A ROOM</h2><p>Create a private code and share the invite with one friend.</p><button disabled={busy} onClick={()=>act("create")}>CREATE ROOM</button></article><form onSubmit={(e:FormEvent)=>{e.preventDefault();act("join")}}><Share2/><h2>JOIN A ROOM</h2><p>Enter the six-character code your friend sent you.</p><input value={code} onChange={e=>setCode(e.target.value.replace(/[^a-z0-9]/gi,"").slice(0,6).toUpperCase())} required minLength={6} placeholder="AB7K2P"/><button disabled={busy}>JOIN ROOM</button></form></section>{error&&<div className="form-error">{error}</div>}</main>}
