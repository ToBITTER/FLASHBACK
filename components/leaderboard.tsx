"use client";import{useEffect,useState}from"react";import{Trophy}from"lucide-react";type Player={rank:number;username:string;totalScore:number;weeklyScore:number;games:number};export function Leaderboard(){const[players,setPlayers]=useState<Player[]|null>(null),[weekly,setWeekly]=useState(false);useEffect(()=>{fetch("/api/leaderboard").then(r=>r.json()).then(d=>setPlayers(d.players)).catch(()=>setPlayers([]))},[]);return<main className="table-page"><header><span>MEMORY CHAMPIONS</span><h1>WHO REALLY<br/>REMEMBERS?</h1><p>Scores only count for signed-in players. Weekly scores roll into a fresh race every Monday.</p></header><div className="table-tabs"><button className={!weekly?"active":""} onClick={()=>setWeekly(false)}>ALL TIME</button><button className={weekly?"active":""} onClick={()=>setWeekly(true)}>THIS WEEK</button></div><section className="score-table"><div className="score-head"><span>RANK</span><span>PLAYER</span><span>GAMES</span><span>POINTS</span></div>{players?.map(p=><article key={p.username}><b>#{p.rank}</b><i>{p.username.slice(0,2).toUpperCase()}</i><strong>{p.username}</strong><span>{p.games}</span><em>{(weekly?p.weeklyScore:p.totalScore).toLocaleString()}</em></article>)}{players?.length===0&&<div className="no-scores"><Trophy/><b>THE TABLE IS OPEN.</b><span>Complete a signed-in game to become the first memory champion.</span></div>}{players===null&&<div className="no-scores">Loading the table…</div>}</section></main>}
