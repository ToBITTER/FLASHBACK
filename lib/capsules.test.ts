import{describe,expect,it}from"vitest";import{capsuleFormats,capsuleScore}from"./capsules";describe("Memory Capsules",()=>{it("supports all seven relationship experiences",()=>expect(Object.keys(capsuleFormats)).toHaveLength(7));it("keeps memory knowledge more valuable than speed",()=>{expect(capsuleScore(true,20000)).toBe(100);expect(capsuleScore(true,0)).toBe(120);expect(capsuleScore(false,0)).toBe(0)})});
